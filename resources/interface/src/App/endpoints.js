const version = '';

export default {
    JOBS: version + 'jobs',
    TAGS: version + 'tags',
    PROVIDERS: version + 'providers',
    SYNC: version + 'server-sync-jobs',
    CITIES: version + 'locations'
}
